import React from 'react'
import DexButton from './DexButton'
import { dexData } from '../DexData'

const Home = () => {
    console.log(dexData)
  return (
    <div>Home</div>
  )
}

export default Home;